
var Person = function(firstAndLast) {
  var nameArr = firstAndLast.split(" ");  
  var first = nameArr[0]; var last = nameArr[1];// var full = firstAndLast;
  
    var getFullName = function() {
           return this.fullName;
        };

    var setFullName = function(firstAndLast) {
          this.fullName = firstAndLast;
        };
  
    var getFirstName = function() {
         return this.firstName;
        };

    var setFirstName = function(first) {
        this.firstName = first;
        };
  
    var getLastName = function() {
         return this.lastName;
        };

    var setLastName = function(last) {
        this.lastName = last;
        };
  
    this.getFullName = getFullName;
};  
  
   
   


var bob = new Person('Bob Ross');
//bob.setFullName("John Doe");
//bob.setFirstName("Haskell");
bob.getFullName();
